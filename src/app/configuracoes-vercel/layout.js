/** Metadados para a página de checklist Vercel. */
export const metadata = {
  title: "Configurações Vercel (online)",
  description: "Variáveis de ambiente e checklist de deploy na Vercel",
};

export default function ConfiguracoesVercelLayout({ children }) {
  return children;
}
