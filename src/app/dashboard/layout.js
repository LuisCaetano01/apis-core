/** Metadados do separador do browser para /dashboard (conversor SSL). */
export const metadata = {
  title: "SSL (ca.pem) converter",
};

export default function DashboardLayout({ children }) {
  return children;
}
