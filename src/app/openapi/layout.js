/** Título/descrição da rota /openapi (Swagger UI). */
export const metadata = {
  title: "OpenAPI (Swagger)",
  description: "Documentação e testes interativos das APIs",
};

export default function OpenApiLayout({ children }) {
  return children;
}
