/** Metadados para /scripts-mysql (referência SQL). */
export const metadata = {
  title: "Scripts MySQL",
  description:
    "Scripts SQL de referência para criar tabelas auth_users e demo_items na base MySQL",
};

export default function ScriptsMysqlLayout({ children }) {
  return children;
}
